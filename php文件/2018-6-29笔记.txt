
休眠函数:sleep():延缓指定秒数后，继续执行后续代码。（此时代码没有执行完成，不会输出）。可以用来查看代码的中间执行过程（临时数据）。

1.文件包含的意义
      php中的文件包含(载入):将部分代码(经常用到的)分离出来,放在单独的文件中,在需要的页面引入对应的文件
      优点:提升代码的重用性,提升开发速度.

2.文件包含函数语法
	需要指定被包含文件所在的路径;
	include 目标文件路径

	include_once 目标文件路径(引入文件,先检测之前有没有被引用,如果有,只	     引用一次)
@屏蔽错误
include引入失败,会出现warning警告,会面的代码会执行

	require 目标文件路径

	require_once 目标文件路径(一次)

而require引入失败会出现error错误,后面的代码不会执行

    引用路径:相对路径,绝对路径

魔术方法: 可以使用 _DIR_(获取当前目录);  _DIR_.'name.php'
3.文件包含原理
	·将被引入文件的代码放在引入位置执行
	·在被引入文件中定义的变量以及常量,可以在引入位置之后直接使用,但是无法在引入位置之前使用
	·引入的文件,单独编译,如果引入的文件中有错误,只有执行到这里才会提示,之前的代码会正常执行.(与整体编译有差别的地方)


4.字符串
    ①获取字符串长度strlen();英文,空格,数字1个字节,汉子3个字节
    ②字符串截取substr('','',字符串)
    ③首字母大写ucfirst()
    ④每个单词首字母大写ucwords()
    ⑤字符串根据某一条件分割explode()
    ⑥字符串转换数组str_split()
    ⑦字符串替换str_replace()
    ⑧计算指定字符串在目标字符串中最后一次出现的位置strrpos()
    ⑨查找字符串首次出现的位置strpos()
    ⑩数组转换成字符串implode()
    11.将所有字母变小写:strtolower()
    12.所有字母变大写:strtoupper()
    13.